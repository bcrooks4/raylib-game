//
// Created by Bill on 20/08/2020.
//

#ifndef RAYLIB_GAME_MACRO_H
#define RAYLIB_GAME_MACRO_H

#define MAX(a, b) ((a) > (b) ? (a) : (b))
#define MIN(a, b) ((a) < (b) ? (a) : (b))
#define CLAMP(a, b, c) (MIN(MAX((a), (b)), (c)))

#endif //RAYLIB_GAME_MACRO_H
